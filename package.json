{
  "name": "fastify-type-provider-effect-schema",
  "version": "0.0.2",
  "description": "@effect/schema Type Provider for Fastify@4",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && npm run typescript && jest",
    "test:coverage": "jest --coverage",
    "lint": "npx rome format . && npx rome check .",
    "lint:fix": "npx rome format --write . && npx rome check --apply .",
    "typescript": "tsd",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@effect/schema": "^0.12.1",
    "fastify": "^4.0.0"
  },
  "repository": {
    "url": "https://github.com/daotl/fastify-type-provider-effect-schema"
  },
  "keywords": [
    "fastify",
    "effect",
    "schema",
    "type",
    "provider"
  ],
  "author": "DAOT Labs",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/daotl/fastify-type-provider-effect-schema/issues"
  },
  "homepage": "https://github.com/daotl/fastify-type-provider-effect-schema",
  "resolutions": {
    "@effect/data": "^0.12.2",
    "@effect/schema": "^0.17.2"
  },
  "devDependencies": {
    "@daotl/rome-config": "0.0.1-alpha.4",
    "@effect/data": "^0.12.2",
    "@effect/schema": "^0.21.0",
    "@types/jest": "^29.5.1",
    "@types/node": "^18.16.0",
    "fastify": "^4.15.0",
    "jest": "^29.5.0",
    "oas-validator": "^5.0.8",
    "rome": "^12.0.0",
    "ts-jest": "^29.1.0",
    "tsd": "^0.28.1",
    "typescript": "^5.0.4"
  },
  "tsd": {
    "directory": "types"
  }
}